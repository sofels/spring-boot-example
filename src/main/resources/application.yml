server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres/customer
    username: postgres
    password: Femoribaba_1
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  main:
    web-application-type: servlet